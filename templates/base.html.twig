<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Cagnot'Potes!{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}

        <!--Import Google Icon Font-->
        <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
        <!--Import materialize.css-->
        <link type="text/css" rel="stylesheet" href="{{ asset('assets/css/materialize.min.css') }}"  media="screen,projection"/>
        
        {% block stylesheets %}
            {#{{ encore_entry_link_tags('app') }}#}
        {% endblock %}
        
        <!--Custom styles-->
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/simplemde/latest/simplemde.min.css">

        <link type="text/css" rel="stylesheet" href="{{ asset('assets/css/styles.css') }}"  media="screen,projection"/>
        {#*********** URL ICON LINK **********#}
        <link rel="icon" href="{{ asset('assets/images/url.png') }}"/>

    </head>
    <body>

        {#*********** NAV BAR **********#}
        <div class="navbar-fixed">
            <nav class="white">
                <div class="nav-wrapper">
                    <a href="{{ path('home') }}" class="brand-logo center">Cagnot'Potes</a>
                    <ul class="right hide-on-med-and-down">
                        <li>
                            <a href="{{ path('campaign_new') }}" class="waves-effect waves-light btn blue-grey lighten-3">Créer une campagne</a>
                        </li>
                    </ul>

                    {# IF CONNECTED LOGOUT ELSE LOGIN #}
                    {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}

                    <ul class="hide-on-med-and-down">
                        <li>
                            <a class="waves-effect waves-light btn deep-orange lighten-1" href="{{ path('app_logout') }}">Logout</a>
                        </li>
                    </ul>

                        {% else %}
                    <ul class="hide-on-med-and-down">

                        <li>
                            <a class="waves-effect waves-light btn deep-orange lighten-1" href="{{ path('app_login') }}"><i class="material-icons">account_circle</i></a>
                        </li>
                    </ul>

                    {% endif %}

                    {# IF NOT CONNECTED REGISTER BTN APPEARS #}
                    {% if not is_granted('IS_AUTHENTICATED_REMEMBERED') %}

                        <li>
                            <a class="waves-effect waves-light btn deep-orange accent-2" href="{{ path('app_register') }}">Register</a>
                        </li>
                        
                    {% endif %}

                    {# RESPONSIVE #}
                    <ul id="nav-mobile" class="sidenav">
                        <li>
                            <a href="{{ path('campaign_new') }}" class="waves-effect waves-light btn deep-orange accent-2">Créer une campagne</a>
                        </li>
                        
                    {# IF CONNECTED LOGOUT ELSE LOGIN #}
                    {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}

                    <ul class="hide-on-med-and-down">
                        <li>
                            <a class="waves-effect waves-light btn deep-orange lighten-1" href="{{ path('app_logout') }}">Logout</a>
                        </li>
                    </ul>

                        {% else %}
                    <ul class="hide-on-med-and-down">
                        <li>
                            <a class="waves-effect waves-light btn deep-orange lighten-1" href="{{ path('app_login') }}"><i class="material-icons">account_circle</i></a>
                        </li>
                    </ul>

                    {% endif %}

                    {# IF NOT CONNECTED REGISTER BTN APPEARS #}
                    {% if not is_granted('IS_AUTHENTICATED_REMEMBERED') %}

                        <li>
                            <a class="waves-effect waves-light btn deep-orange accent-2" href="{{ path('app_register') }}">Register</a>
                        </li>
                        
                    {% endif %}
                    </ul>
                    <a href="#" data-target="nav-mobile" class="sidenav-trigger"><i class="material-icons">menu</i></a>
                </div>
            </nav>
        </div>
        {% block body %}{% endblock %}

        {#*********** FOOTER **********#}
        <footer class="page-footer blue-grey lighten-2">
            <div class="container">
                <div class="row">
                    <div class="col l6 s12">
                        <h5 class="white-text">Cagnot'Potes</h5>
                        <p class="grey-text text-lighten-4">Lorem ipsum dolor sit amet, consectetur adipiscing elit. Nullam scelerisque id nunc nec volutpat. Etiam pellentesque tristique arcu, non consequat magna fermentum ac. Cras ut ultricies eros. Maecenas eros justo.</p>
                    </div>
                    <div class="col l3 s12 offset-l3">
                        <h5 class="white-text">Liens <i class="material-icons">link</i></h5>
                        <ul class="footer-links">
                            <li><a class="white-text" href="#!">Comment ça marche ?</a></li>
                            <li><a class="white-text" href="{{ path('campaign_index') }}">Voir les campagnes</a></li>
                            <li><a class="white-text" href="{{ path('campaign_new') }}">Créer une campagne</a></li>
                        </ul>
                    </div>
                </div>
            </div>
            <div class="footer-copyright">
                <div class="container">
                    Made with love by Eléa<i class="material-icons">copyright</i></a>
                </div>
            </div>
        </footer>
      
        <!--JavaScript at end of body for optimized loading-->
        <script type="text/javascript" src="{{asset('assets/js/materialize.min.js')}}"></script>  
        {% block javascripts %}
            {#{{ encore_entry_script_tags('app') }}#}
        {% endblock %}
        <!--Init & custom scripts-->
        <script src="https://cdn.jsdelivr.net/simplemde/latest/simplemde.min.js"></script>

        <script type="text/javascript" src="{{asset('assets/js/scripts.js')}}"></script>
    </body>
</html>
